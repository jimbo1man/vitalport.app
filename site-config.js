/** Public VitalPort URLs: referenced from HTML via data-vp-href; keep fallback href in markup in sync. */
window.VITALPORT_LINKS = Object.freeze({
  appStore: 'https://apps.apple.com/app/id6764275336',
  manual: '/manual/',
  privacy: '/privacy/',
});
